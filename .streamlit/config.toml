[theme]
primaryColor = "#6CB4A4"           # Purple (mental wellness)
backgroundColor = "#FAFAFA"        # Pure white (sobriété luxueuse)
secondaryBackgroundColor = "#F5FAF9"  # Light gray for cards
textColor = "#1A202C"              # Dark gray
font = "sans serif"

[server]
headless = true
port = 8501
