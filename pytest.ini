[pytest]
# Configuration pytest pour Serene

# Chemins de test
testpaths = tests

# Options par défaut
addopts =
    -v
    --strict-markers
    --tb=short
    --disable-warnings

# Markers personnalisés
markers =
    slow: marque les tests lents
    integration: marque les tests d'intégration
